#' Create a config file to use as guidance for metabolism runs
#' 
#' @import mda.streams
#' 
#' @param config a config for the config
create_metab_config <- function(config=yaml::yaml.load_file('2_metab_config/in/metab_configs_config.yml')) {
  
}
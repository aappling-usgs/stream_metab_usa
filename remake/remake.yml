# Ensures that the entire project is up to date

packages:
  - remake

includes:
  - 1_site_data.yml
  - 1_ldas_host.yml
  - 1_timeseries.yml
  - 4_data_release.yml

targets:
  all:
    depends: ../1_site_data/out/site_list.txt

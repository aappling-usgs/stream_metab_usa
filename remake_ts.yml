packages:
  - yaml
  - dplyr
  - XML
  - foreach
  - mda.streams
  
## All R files that are used must be listed here:
sources:
  - p1_import/code/02_add_ts_data.R

targets:
  all:
    depends: 
      - baro_nldas
  
  ts.config:
    command: yaml.load_file("configs/nldas_ts.yml")
    
  baro_nldas:
    command: stage_ts(target_name, ts.config)
    
  
  
  ##posted.baro:
  ##  command: post_ts(baro, on.replace)
